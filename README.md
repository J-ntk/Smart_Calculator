\# Smart CLI Calculator



A simple command-line calculator built with Python.



\## Features

\- Basic operations (+, -, *, / , ** , % , sqrt)

\- Error handling

\- Saves calculation history

\- Modular structure



\## How to run

```bash

python main.py


# Smart Calculator v2

**Smart Calculator v2** is an advanced Python command-line calculator that supports:

- Basic arithmetic operations: addition (`+`), subtraction (`-`), multiplication (`*`), division (`/`)
- Advanced operations: exponentiation (`**`), modulo (`%`), and square root (`root()`)
- Multi-number calculations in **one expression** (e.g., `5 + 3 * 2`)
- Continuous calculation without restarting
- Calculation **history** saved to a file (`history.txt`)
- User-friendly interface with input validation

---

## Features

1. **Multi-number, multi-operation support**
   - You can enter complex expressions with multiple numbers and operators in one line.
   
2. **Root operation**
   - Supports calculating square roots directly (`root(number)`).

3. **History tracking**
   - All calculations are logged to `history.txt`.
   - You can view history at any point by typing `h`.

4. **User-friendly interface**
   - Input is validated to prevent errors.
   - Supports quitting at any point (`q`).

---

## Getting Started

### Prerequisites

- Python 3.10 or higher

### Installation

1. Clone the repository:

```bash
git clone https://github.com/J-ntk/Smart_Calculator.git

